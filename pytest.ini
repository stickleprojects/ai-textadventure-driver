[pytest]
addopts = -m "not llm and not cloud"
markers =
    llm: requires local llama.cpp model (run with -m llm)
    cloud: requires a real cloud LLM API key and makes real network calls (network + cost; run with -m cloud)
    integration: requires game binary
    slow: long-running
    ui: Streamlit AppTest UI smoke tests
