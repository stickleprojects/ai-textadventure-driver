# Requirement 10b: Score parsing

**Status:** Fixed

The agent needs to track its score to measure progress across runs.

**Fix:** Agent issues `score` command every 20 steps; response parsed into `state["current_score"]` / `state["max_score"]`.
