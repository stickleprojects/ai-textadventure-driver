# Requirement 6: Agent cannot wear items or disguises

**Status:** Fixed

The engine needs to know it can wear things and should wear disguises (the hood for example).

**Fix:** Superseded by requirement 18 — `wear` is now in `candidate_verbs` and is attempted on every discovered object.
