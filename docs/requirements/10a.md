# Requirement 10a: Death and end-state detection

**Status:** Fixed

The agent needs to detect death, game completion, and other end states so runs can be classified correctly.

**Fix:** `end_state_patterns` in `knight_orc.json`; `run_evaluator.classify_run()` returns one of six outcomes.
