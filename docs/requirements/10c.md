# Requirement 10c: Cross-run learning

**Status:** Open

The agent needs to accumulate knowledge across runs rather than starting from scratch each time. See also requirement 21 (timed multi-step strategies) for a concrete motivating example.

**Scope:** utility tagging per step, `room_policies` / futility threshold, `runs/` persistence, `knight_orc_strategy.json` merge, startup load.

**Effort: Very High | Risk: Very High**

For the full design, see the [plan-issue10](../../memory/plan_issue10.md) project memory entry.
