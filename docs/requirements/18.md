# Requirement 18: Fixed inspection sequence for all objects

**Status:** Fixed

The agent applied the same inspection sequence to every object regardless of type.

**Fix:** Replaced fixed `inspection_sequence` with `candidate_verbs` (15 verbs); records `succeeded`, `blocked`, or `invalid` per verb per entity.
