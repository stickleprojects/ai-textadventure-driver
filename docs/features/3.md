# Feature 3: Graph boxes don't scale, no zoom

**Status:** Fixed

The boxes for the graph don't scale and there's no zoom so you can't read the text.

**Fix:** Replaced matplotlib with pyvis interactive graph (zoom, pan, drag; current room highlighted in amber).
