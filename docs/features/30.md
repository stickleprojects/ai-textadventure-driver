# Feature 30: No clean way to break out of watch_run.py

**Status:** Fixed

There was no way to break out of `watch_run.py` while ensuring it closes cleanly and writes the logs.

**Fix:** `except KeyboardInterrupt` saves logs, appends `INTERRUPTED` entry, classifies run as `"interrupted"`.
