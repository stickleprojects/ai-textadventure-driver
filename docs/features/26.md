# Feature 26: No pipeline or quality gates

**Status:** Fixed

Add pipeline and quality gates to ensure docs are good standard and the Python tests pass.

**Fix:** GitHub Actions CI (lint + test jobs), ruff config in `pyproject.toml`, JSON schemas for all config/plan/eval files, `tests/test_json_schemas.py` (PR #48).
