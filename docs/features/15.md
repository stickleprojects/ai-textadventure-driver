# Feature 15: Map visualization doesn't word wrap room names

**Status:** Fixed

The map visualization doesn't word wrap the room descriptions.

**Fix:** `_display_label()` abbreviates `Unknown (X from Y)` to `? X` and word-wraps long names at 20 chars.
