# Feature 42: watch_run.py doesn't print run ID at startup

**Status:** Fixed

`watch_run.py` does not print the run ID at startup, making it hard to cross-reference logs.

**Fix:** Run ID printed to stderr immediately after game start.
