# Feature 28: watch_run.py lacks per-step timing metrics

**Status:** Fixed

`watch_run.py` takes ages for each step and there was no way to see progress or estimate remaining time.

**Fix:** Each verbose line now shows `[elapsed Xm00s | X.Xs/step | remaining Xm00s]`.
