# Feature 9: Map doesn't draw rooms in cardinal direction

**Status:** Fixed

The visualisation of the map doesn't draw the boxes in cardinal direction.

**Fix:** BFS from first discovered room assigns pixel positions from edge direction labels; physics disabled so nodes stay pinned.
