# Bug 33: "Exits lead in all directions" not parsed

**Status:** Fixed

The prompt needed to understand "Exits lead in all directions." and expand it to a full exit list.

**Fix:** Prompt rule expands this to `["north", "south", "east", "west", "up", "down"]`; eval cases added.
