# Bug 5: Agent assumes seen items are in inventory

**Status:** Fixed

The engine appears to assume that if it can see an item, it is in the inventory — it needs to take things to have them in the inventory.

**Fix:** `known_entities` status updated from `discovered` → `held` when `added_to_inventory` fires; overlapping prompt fix landed in bug 4.
