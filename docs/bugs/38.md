# Bug 38: LLM hallucinates direction string as room name

**Status:** Fixed

We see a location of "propet_northeast" — LLM hallucinating a direction string as a room name.

**Fix:** Prompt now gives concrete counter-examples: directions, character names, object names, and descriptors are all prohibited as room values; only a named place is valid.
