# Bug 39: LLM places NPC name in room field

**Status:** Fixed

We see a location of "Denzyl" — LLM placing an NPC name in the `room` field instead of `npcs`.

**Fix:** Same prompt rule as bug 38; character names explicitly listed as invalid room values; eval case added to verify `Denzyl` appears in `npcs` and not `room`.
