# Bug 1: Engine reads and inspects horse as object

**Status:** Fixed

The engine was reading the horse, and looking inside the horse — it should recognise that a horse cannot be read.

**Fix:** LLM schema now separates `npcs` (living creatures) from `objects` (inanimate items); NPCs are routed to `known_npcs` state and never enter the inspection queue.
