# Bug 12: Engine still tries to read the horse

**Status:** Fixed

After bug 1 fix, the engine was still trying to read the horse — creatures were still entering the inspection queue.

**Fix:** `_is_creature()` pre-filter in `process_agent_step` blocks creature-named items from entering `uninspected_objects`; any creature-word match is routed to `known_npcs` instead.
