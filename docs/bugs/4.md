# Bug 4: "You can see" not parsed as room contents

**Status:** Fixed

The engine needs to understand "you can see" and interpret that as things in the current room.

**Fix:** Prompt now explicitly states "you can see X" means X is in the current room (objects/npcs), not inventory.
