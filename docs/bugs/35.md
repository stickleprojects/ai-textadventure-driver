# Bug 35: LLM hallucinates inventory on hard-failure responses

**Status:** Fixed

LLM hallucinates `added_to_inventory` on hard-failure responses such as "You don't need to use the word X".

**Fix:** `extracted.pop("added_to_inventory", None)` on hard failure before log entry is written; eval case and regression tests added.
