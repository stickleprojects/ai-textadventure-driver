# Bug 11: Timeout message lacks raw received text

**Status:** Fixed

The warning "command timed out" message was unhelpful — it didn't show what was actually received, making it impossible to use for debugging or updating rules.

**Fix:** Timeout now includes the raw partial output received before pexpect gave up; Streamlit file watcher disabled to prevent coredumps; `action_taken` wired into LLM extraction prompt.
