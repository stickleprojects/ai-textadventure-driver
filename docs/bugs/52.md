# Bug 52: Map PNG uses dark theme, hard to read

**Status:** Fixed

Map PNG used dark theme (dark background, purple edges, purple labels) making it hard to read when printed or shared.

**Fix:** `save_graph_image` now uses white background, black edges, black edge labels on transparent background.
