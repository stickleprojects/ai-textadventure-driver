# Bug 32: Crash details not visible in log file

**Status:** Fixed

Crash details were not visible in the log file.

**Fix:** Crash appended to `state["game_log"]` as a `utility: "crash"` entry before the log is written.
