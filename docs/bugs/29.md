# Bug 29: Command echo appearing in game responses

**Status:** Fixed

Command echo was appearing in game responses — pexpect includes a leading `\r\n` before the echoed command in some terminal modes.

**Fix:** `lstrip('\r\n ')` before the startswith check in `execute_game_command`; regression test added.
