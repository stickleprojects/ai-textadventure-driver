# Bug 36: Wear command produces bad location string

**Status:** Fixed

After issuing "wear flagpole" we get a location of "current location not explicitly named".

**Fix:** Prompt now explicitly instructs the LLM to set `room` to null if the response does not name a location.
