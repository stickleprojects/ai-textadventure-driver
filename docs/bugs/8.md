# Bug 8: NoneType error on null potential_solution

**Status:** Fixed

`anomaly_data.get("potential_solution", "").lower()` throws `NoneType has no attribute lower` when the LLM returns `null` for `potential_solution`.

**Fix:** Normalise to `""` at storage time in `process_agent_step` so `None` never enters state.
