# Bug 37: Examine command sets room to object name

**Status:** Fixed

After issuing "examine flagpole" we get a location of "flagpole", which is not correct.

**Fix:** Same prompt rule as bug 36; `room` must be null when no place is named.
