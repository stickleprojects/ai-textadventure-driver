# Bug 14: Loop detection false-positive on object inspection

**Status:** Fixed

The loop detection was suspicious — looking at lots of items in the inventory would fail the loop check even though that is legitimate agent behaviour.

**Fix:** Direction actions that produce a room change are excluded from the loop count (productive navigation); object inspection never false-positives because each action includes the object name as a unique string.
